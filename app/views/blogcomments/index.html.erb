<div class="prepend-1">
	<%= link_to "Dodaj wpis", new_user_blogpost_path(current_user) %>
<% unless @user.blogposts.empty? %>
        <table summary="User blogposts">
          <%= render @blogposts %>
        </table>
</div>
        <%= will_paginate @blogposts %>
<% end %>