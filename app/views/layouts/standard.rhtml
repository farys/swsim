<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title><%= yield :title %> <%= @global_title %></title>
  <%= render 'layouts/stylesheets' %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  
  
  <%= yield :head %>
</head>
<body>
 <div id="site">
  <div id="logo"><span class="shadow">inż v0.1</span>
    <div id="topMenu">
      <%= render :partial => "layouts/top_menu" %>
    </div>
  </div>
  <% if flash.key? :notice -%><p id="flashNotice"><%= flash[:notice] %></p><% end -%>
  <% if flash.key? :error -%><p id="flashError"><%= flash[:error] %></p><% end -%>
  <% if flash.key? :warning -%><p id="flashWarning"><%= flash[:warning] %></p><% end -%>
  <div id="content">
    <%= yield %>
    <%= debug(params) if Rails.env.development? %>
  </div>
  <div style="clear: both"> </div>

</div>
</body>
</html>

