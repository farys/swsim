<div class="prepend-1 append-1">
  <% form_for [:admin, @tag] do |f| -%>

    <%= render "layouts/error_messages", :target => @tag %>

    <p>
      <%= f.label :name %>
      <%= f.text_field :name, :class => "title" %>
    </p>

    <p>
      <%= f.label :status %>
      <%= select :tag, :status, statuses_for_select(@tag) %>
    </p>

    <%= f.submit "Zmień" %>
  <% end -%>
</div>